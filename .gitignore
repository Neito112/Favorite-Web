# 1. Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# 2. Dependency directories (Thư viện tải về - KHÔNG UP)
node_modules
jspm_packages

# 3. Build outputs (File đóng gói - KHÔNG UP)
dist
dist-electron
release
build

# 4. System Files (File rác hệ điều hành)
.DS_Store
Thumbs.db
Desktop.ini

# 5. Environment variables (Bảo mật)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 6. IDE files (Cấu hình riêng của VSCode/WebStorm)
.idea
.vscode
*.swp
*.swo

# 7. Electron Builder config (File tạm khi build)
electron-builder-effective-config.yaml